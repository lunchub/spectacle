%li.field.nested-fields
  = f.input_field :connected_page_id, collection: Page.where.not( id: [f.object.page.try(:id), PageConnectedPage.where(connected_page_id: f.object.page.id).first.try(:id)] ), class: 'input'
  %div.danger.btn.small
    = link_to_remove_association "削除", f
