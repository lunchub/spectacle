= simple_form_for(@question_comment) do |f|
  = f.error_notification

  .form-inputs
    = f.input :comment
    = f.association :question
    = f.association :user

  .form-actions
    = f.button :submit
