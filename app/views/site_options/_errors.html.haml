%div.row
  %div.twelve.columns
  - if @site_option.errors.any?
    #error_explanation.alert.danger
      %h3= "#{pluralize(@site_option.errors.count, "error")} prohibited this page from being saved:"
      %ul
        - @site_option.errors.full_messages.each do |msg|
          %li= msg
