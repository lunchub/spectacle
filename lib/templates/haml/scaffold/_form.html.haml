= semantic_form_for @<%= singular_name %>, html: {id: '', class: ''} do |f|
  = f.inputs do
  <%- attributes.each do |attribute| -%>
    = f.input :<%= attribute.name %>
  <%- end -%>

  = f.actions do
    = f.action :submit, :as => :input
